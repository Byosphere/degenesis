import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.degenesis.app',
  appName: 'degenesis',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
